# ================================== #
#         Goonio Configuration       #
# ================================== #
# Copy this file to .env and change the values as needed.
# On platforms like Render, these should be set in the environment variables section.

# --- Server Settings ---
# The log level for the application. Can be DEBUG, INFO, WARNING, ERROR.
LOG_LEVEL=INFO

# The port the application will run on inside the container.
# This should match the port exposed in the Dockerfile and render.yaml.
FASTAPI_PORT=8000
